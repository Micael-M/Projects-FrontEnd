import React from 'react';

const Layout = ({ children }) => {
  return (
    <section>
    <p> Olá, brasiiiiiil! O componente layout está pegando</p>
    { children }
  </section>
  );
};

export default Layout;
